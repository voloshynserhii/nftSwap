import React from 'react';

import LogoIcon from '../../assets/icons/logo.png';

const Logo = props => {
    return (
        <div>
            <img src={LogoIcon} alt="logo" />
        </div>
    );
};

export default Logo;